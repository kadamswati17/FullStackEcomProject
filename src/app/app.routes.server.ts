// Optional server route configuration for Angular SSR.
// If you install `@angular/ssr`, uncomment and adjust the imports below.
// import { RenderMode, ServerRoute } from '@angular/ssr';

// export const serverRoutes: ServerRoute[] = [
//   {
//     path: '**',
//     renderMode: RenderMode.Prerender
//   }
// ];
